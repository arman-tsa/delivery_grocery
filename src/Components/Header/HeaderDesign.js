import React from 'react';
import { Text, View } from 'react-native';

const HeaderDesign = ({
    params,
}) => (
    <View>
        <Text>HeaderDesign</Text>
    </View>
);

export default HeaderDesign;
